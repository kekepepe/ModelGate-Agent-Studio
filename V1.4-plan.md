# V1.4 执行计划 — Parallel Multi-Agent Runtime（单机强化）

> 立项于 2026-09-16。前置：V1.3 完成。**用户已拍板：单机 ThreadPool 强化路线，
> 不引入 Redis/arq**（探索证实并行雏形已可用；单机场景下跨进程 worker 池是为架构而架构）。
> 约定不变：每子任务 1 commit + 1 push。

---

## 0. 现状盘点（探索证实）

已有且可用：`parallel_safe` 任务分组、`Goal.max_parallel_tasks` + `AgentStation.max_concurrency`
容量选择、每线程独立 SQLAlchemy session、git worktree 隔离 + 串行 merge + 冲突→replan、
lease 恢复（过期扫描 + 非幂等工具 → waiting_approval）、DB 状态 pause/resume。

真实 gap（本阶段全部补齐）：
1. **恢复只在 pipeline 启动时跑**——宿主进程死掉后，恢复要等下一次有人手动 execute
2. **resume 从头重跑**——resume 把 running→pending，任务从零开始（幂等保证安全但浪费）
3. **merge 顺序不确定**——`as_completed` 收集，合并顺序非依赖序
4. **SQLite 写竞争**——并行 session 写同一文件，无 WAL
5. **并行可观测性弱**——parallel_group_started 有日志，无面板/断言面

## 1. 设计决策

- **D1** 不引入 Redis/arq（ROADMAP 修订：移至 V2.0 按需）。单机 ThreadPool 强化覆盖
  "无依赖 Task 并行 + 依赖等待 + 容量限制 + 恢复"的全部验收场景。
- **D2** 恢复常态化用**进程内后台线程**（daemon，每 60s 扫描 + 心跳续租），不改架构——
  arq 级别的跨进程恢复留给 V2.0 按需。
- **D3** resume 增量化以 `WorkspaceCheckpoint` 为断点：checkpoint 之后未写入的变更按
 幂等规则重放，已 checkpoint 的跳过；无 checkpoint 的任务维持从头重跑（诚实降级）。
- **D4** SQLite 开 WAL（`PRAGMA journal_mode=WAL`，database.py 引擎事件一次性设置）；
  Postgres 切换仍走 compose profile（已存在），不在本阶段做代码级适配。

## 2. 任务清单

- [ ] T1 SQLite WAL：`database.py` 引擎 connect 事件设置 `journal_mode=WAL` +
      `busy_timeout`；并行写竞争回归测试（多线程同时 commit 压力）
- [ ] T2 恢复常态化：`recovery_service.start_recovery_daemon()`（daemon 线程，60s 周期：
      心跳续租 in-flight lease + 过期扫描恢复），main.py 启动挂载 + 优雅停止；
      `recovery.last_scan` 事件可观测
- [ ] T3 resume 增量化：`_execute_single_task` 检测到 resumed-from-pending 任务时，从最近
      WorkspaceCheckpoint 恢复文件状态后再跑（复用 `_restore_task_checkpoints` 的
      校验和验证）；无 checkpoint → 诚实从头重跑并记日志
- [ ] T4 调度确定性：`_merge_parallel_worktrees` 按依赖序（拓扑序）排序合并；
      `_execute_parallel_tasks` 结果收集按传入 task_ids 顺序（去 as_completed 乱序）
- [ ] T5 并行预算透出：`GET /api/v1/goals/{id}` 返回 max_parallel_tasks /
      per-station max_concurrency；run 状态含并行组历史
- [ ] T6 `scripts/e2e-parallel.sh`：构造含 ≥2 个 `parallel_safe` coding task 的 Goal
      （mock 模式）→ 断言 `parallel_group_started` 日志、双 worktree 创建、merge 成功、
      文件互不污染、goal completed；含一个故意冲突的用例 → 断言 blocked + replan
- [ ] T7 收尾：CHANGELOG + HANDOVER 快照更新

## 3. 验收标准

1. 压力测试：8 线程并发 commit 无 "database is locked"
2. 杀掉执行中的 backend 进程 → 重启 → daemon 在 60s 内恢复过期任务
   （幂等工具自动重跑 / 非幂等 → waiting_approval）
3. e2e-parallel.sh 双场景（并行成功 + 冲突 blocked）PASS
4. pause → resume 后，已 checkpoint 的变更不重做（断言 checksum 一致）

## 4. 明确不做

- Redis/arq/多机（V2.0 按需）；分布式 merge；流式 Anthropic adapter（若 V1.3 未做）
- Goal 级并行（一个 Goal 内多 Task 并行即可，多 Goal 并行不在本阶段）
