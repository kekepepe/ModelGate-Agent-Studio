# ModelGate Agent Studio 部署指南

> 当前部署形态：FastAPI + SQLite + React 静态构建 + Nginx。

## Docker Compose 部署

```bash
git clone <repository-url> modelgate-agent-studio
cd modelgate-agent-studio
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env
docker compose up -d --build
docker compose ps
```

访问 `http://<服务器地址>/`。前端 Nginx 将 `/api/v1` 代理到内部 `backend:8000`，无需公开后端端口。

查看日志与停止：

```bash
docker compose logs -f backend
docker compose logs -f frontend
docker compose down              # 保留 SQLite volume
# docker compose down -v         # 删除数据，谨慎使用
```

## 本地一键启动与代码热更新

日常本地开发请使用开发 Compose，而不是上面的生产式 Compose：

```bash
cd /Users/kepeng/codex_project/ModelGate\ Agent\ Studio
docker compose -f docker-compose.dev.yml up --build
```

启动后：

- 前端：<http://localhost:5173>
- 后端 API：<http://localhost:8000/api/v1>
- 后端 health：<http://localhost:8000/health>

首次使用需要构建镜像；之后通常不需要 `--build`：

```bash
docker compose -f docker-compose.dev.yml up
```

`docker-compose.dev.yml` 已挂载 `backend/src` 与 `frontend/src`：

| 修改内容 | 是否实时生效 | 机制 |
|---|---|---|
| `frontend/src/**` | 是 | Vite HMR 自动刷新页面。 |
| `backend/src/**` | 是 | Uvicorn `--reload` 自动重启后端进程。 |
| `frontend/package.json`、`backend/requirements.txt`、Dockerfile、Compose 配置 | 否 | 需要停止后执行 `docker compose -f docker-compose.dev.yml up --build`。 |
| 数据库 schema / migration | 不一定 | 当前仍处于 migration 切换期；按 migration 计划处理，不要仅依赖热重载。 |

停止本地开发环境：

```bash
docker compose -f docker-compose.dev.yml down
```

注意：`docker compose up --build`（不带 `-f docker-compose.dev.yml`）使用的是生产式镜像，前端文件已打包进 Nginx，后端也没有 `--reload`，修改代码后**不会**实时生效；需要重新 build 并重启。

## 真实模型 Provider

正式 Goal 默认使用 `EXECUTION_MODE=live`；Mock 只可由用户明确选择用于测试或离线演示。当前 Provider 是 OpenAI-compatible：

```bash
export EXECUTION_MODE=live
export OPENAI_BASE_URL=https://api.openai.com/v1
export OPENAI_API_KEY='replace-with-secret'
export OPENAI_TIMEOUT=60
docker compose up -d --build
```

不要把 API Key 写进 Dockerfile、前端 `VITE_*` 变量或 Git；生产环境请用 Docker secret、Vault 或云端 secret manager。

## Workspace Sandbox

`sandbox` 模式始终执行路径和命令白名单策略。默认 `SANDBOX_BACKEND=local`，适合 Compose 部署：后台容器**不会**默认获得宿主 Docker socket。

在受控的宿主机进程中，可显式启用隔离 Docker Runner：

```bash
export EXECUTION_MODE=sandbox
export SANDBOX_BACKEND=docker
export SANDBOX_IMAGE=python:3.12-slim
```

Docker Runner 以禁网、`cap-drop=ALL`、`no-new-privileges`、进程数上限和单一 Workspace 挂载运行命令。不要为了在 Compose 容器内启用它而直接挂载 Docker socket；那会把宿主机级权限交给 Agent Runtime。若确需容器化 Docker Runner，应单独部署具备审计和最小权限的远程 Runner。

## 数据备份

SQLite 位于 `modelgate-data` volume 的 `/app/data/modelgate.db`。升级前先备份：

```bash
docker compose stop backend
mkdir -p backups
docker run --rm -v modelgate-agent-studio_modelgate-data:/data:ro \
  -v "$PWD/backups:/backup" alpine \
  sh -c 'cp /data/modelgate.db /backup/modelgate-$(date +%Y%m%d-%H%M%S).db'
docker compose start backend
```

## 生产上线门槛

当前项目可用于单机 MVP 验证；公网生产前必须完成：

1. `DEBUG=false`，并将 CORS 从本地 5173/5174 改为正式域名或保持同域代理。
2. HTTPS、限流、安全响应头和认证授权。
3. 备份/恢复演练；多副本或高并发时迁移至 PostgreSQL。
4. 真实 Provider 的错误分类、重试、超时、脱敏日志和密钥管理。
5. 按 [Migration 切换计划](../implementation/database-migration-cutover-plan.md) 接入版本化 migration；不要对既有库回放历史 `migrations/*.sql`。

## 发布验收

- [ ] frontend 与 backend 均运行；前端可通过同域 `/api/v1` 调用 API。
- [ ] 可以新建并执行 `Plan → Build → Review` Workspace Goal。
- [ ] Live 模式已通过 Model Manager 的健康检查，并在 Execution Log 中记录真实 provider、model、request id、token 与耗时。
- [ ] Sandbox 模式已在目标运行环境执行一次隔离命令；不因默认 Compose 配置而暴露 Docker socket。
- [ ] Provider Key 未出现在前端产物、日志或 Git diff。
- [ ] SQLite volume 已备份。
- [ ] 后端 pytest、前端测试、生产构建均通过。
