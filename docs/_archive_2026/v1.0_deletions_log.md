# V1.0 重构期间的删除/归档日志

> 用途：记录 V1.0-1/2/3 期间**被删除或归档**的文件 / 目录，以及**为何删 / 如何重建**。
> 这份日志本身**不会被删**——它是"再启用"旧代码时的入口。
> 维护原则：每次再启用一处旧代码，就把对应条目从这里移到"已重建"列表。

---

## 0. 时间线

| 日期 | commit | 摘要 |
|---|---|---|
| 2026-09-06 | `56dd1de` | 99 份规划文档归档到 `_archive_2026/`（V1.0-1） |
| 2026-09-06 | `1079a86` | 新建 `src/providers/` + `src/runtime/` 入口（V1.0-2） |
| 2026-09-06 | `5c5d083` | Settings 加 provider 字段（V1.0-2 followup） |
| 2026-09-06 | `59bf1b9` | Station 增 slug/is_builtin/handoff_policy（V1.0-3） |
| 2026-09-06 | _本 commit_ | 删 3 个文件 + 归档 3 个顶层 markdown（V1.0-3 整理） |

---

## 1. 已删除（V1.0 重构期间）

### 1.1 `backend/src/services/mock_provider.py`

- **删除 commit**：_本 commit_（V1.0-3 整理）
- **删除原因**：
  - 0 个生产代码 import（仅同名文件 `src/services/providers/mock_provider.py` 被 V1.0-2 取代）
  - 功能完全被 `src/providers/mock_provider.py` 替代（V1.0-2 新建）
- **如何重建**（如果将来需要老的行为）：
  - 老文件结构是单一 `MockProvider` class，~120 行，签名 `chat(prompt, model, system_prompt) -> str`
  - 新版本在 `src/providers/mock_provider.py`，签名遵循 `LLMProvider` Protocol（`async chat(messages, model, ...)`），含 streaming
  - **不可一行命令恢复**；如需精确复制，见 commit `59bf1b9` 之前的历史

### 1.2 `backend/src/routes/dashboard.py`

- **删除 commit**：_本 commit_
- **删除原因**：
  - 设计文档 §5 明确移除顶层 `Dashboard` 顶层导航
  - 设计中 `Workspace Overview`（`/workspace`）承担"运行总览"职责
  - 文件 ~70 行，简单 FastAPI 路由 + SQLAlchemy 查询
- **如何重建**：
  - 新建 `backend/src/routes/workspace_overview.py` 即可
  - 数据源：复用 `workspace_service.list_runs(...)`（已存在）
  - 模板层：前端 `pages/WorkspaceOverviewPage.tsx` 已存在，重连即可

### 1.3 `backend/src/models/selection.py`

- **删除 commit**：_本 commit_
- **删除原因**：
  - 当前 0 个生产代码 import（V1.0-3 之前 grep 验证过）
  - 服务于多 Agent 选 Agent / 选 Model 决策，V1.4+ 才用
  - 设计文档 V1.x 范围内无对应实体
- **如何重建**（V1.4 启用多 Agent 选型时）：
  - 简单 SQLAlchemy model（44 行）：`AgentSelection(id, agent_id, model_id, selection_reason, score_breakdown, created_at)`
  - Alembic 迁移 `0011_agent_selection.py`：1 张新表
  - 服务层 `agent_selector_service.py` 已有完整实现，直接 import 即可
  - 关联设计：见 `docs/design/2026-09-06-platform-redesign.md` §4.4 Router

---

## 2. 已归档（未删，仅挪位置）

### 2.1 `CHANGELOG.md` → `docs/_archive_2026/CHANGELOG.md`

- **归档 commit**：_本 commit_
- **归档原因**：内容是 2026-07-18 RC 候选版本日志，V1.0-8 收尾时会按新格式重写
- **如何重建**：参考新设计 §8/§9 节奏 + 现有 `docs/CHANGELOG.md` 内容

### 2.2 `RELEASE_NOTES.md` → `docs/_archive_2026/RELEASE_NOTES.md`

- **归档原因**：RC 验收期临时记录，V1.0-8 收尾时按发布模板重写
- **如何重建**：参考 `docs/review/2026-07-18-RC验收报告.md`（已归档在 `_archive_2026/review/`）

### 2.3 `design-qa.md` → `docs/_archive_2026/design-qa.md`

- **归档原因**：Dual Workspace 设计 QA 是 P0-P7 时期的副产物，V1.0-6 时会被新的 Design QA 替换
- **如何重建**：参考 `output/playwright/dual-workspace-qa-comparison.png` + `output/playwright/workspace-{card,pixel}-final.png`（旧素材，保留在 `output/playwright/`）

---

## 3. 保留但记录"暂时不能动"

### 3.1 `backend/src/services/providers/`（整个包，6 文件）

- **保留原因**：
  - 7 处生产代码引用：`review_service.py`、`model_orchestrator_service.py`、`runtime_service.py`、`embedding_service.py`、`model_gateway.py`、自己包内
  - 4 处测试引用：`test_mock_provider.py`、`test_runtime_e2e.py`、`test_model_orchestrator.py`、`conftest.py`
  - 是 V1.0-4 Runtime 引擎的**直接依赖**
- **什么时候可以删**：
  - 当 `runtime_service.py` / `orchestrator_service.py` 全部迁移到用新 `src/providers/` 时（V1.0-4 后期或 V1.0-5）
  - 迁移步骤：每个 service 改 import → 调新 provider → 跑测试 → 删老 import → 全部通过后整个 `services/providers/` 删
- **替代品**（已就绪）：
  - `src/providers/base.py` `LLMProvider` Protocol
  - `src/providers/litellm_provider.py` LiteLLM 实现
  - `src/providers/mock_provider.py` Mock 实现
- **两者协议差异**：
  - 老：`sync chat(prompt, model, system_prompt) -> str` + `configure_model(model_id, output_text_fn)`
  - 新：`async chat(messages: list[dict], model, *, tools=None, ...) -> dict` + `stream_chat` + `token_count`
  - 签名是破坏性变更，迁移需要改调用方

### 3.2 `backend/migrations/`（整个目录，9 SQL 文件）

- **保留原因**：
  - `tests/test_alembic_migrations.py::test_legacy_012_database_upgrades_without_losing_runtime_records` 引用这 9 个 SQL 做"老库升级"测试
  - 模拟"用户从老版本（v0.12 时代）升级到 V1.0"的场景
  - Alembic 0001 已经是 baseline，但老 SQL 文件包含 v0.12 之前的手写 schema 演进
- **什么时候可以删**：
  - 当 `test_legacy_012_database_upgrades_without_losing_runtime_records` 被改成用 Alembic `0001_schema_baseline` 起步（"老库"=V1.0 之前的某次 Alembic 状态）
  - **不建议删**：这是唯一一个能模拟"非 Alembic 时代数据库升级"的测试
- **替代品**：无；这是历史包袱但有测试价值

### 3.3 `scripts/`（2 bash 脚本）

- `scripts/api-smoke.sh`：3 行 curl 验证 backend 起得来
- `scripts/wait-for-services.sh`：60 秒内等 backend + frontend 都就绪
- **保留原因**：V1.0-7 / V1.0-8 做 E2E demo 时直接用
- **什么时候可以删**：永不（轻量、长期有用）

### 3.4 老 `modelgate.db` / `test.db`（根目录）

- **状态**：已 `.gitignore` 屏蔽（`*.db` 规则），未 commit
- **本地开发继续用**，不需要动

---

## 4. 已被新设计取代的旧代码（V1.0-2/3 已替换）

| 旧 | 新 | 替换 commit |
|---|---|---|
| `services/mock_provider.py`（非包内那个） | `src/providers/mock_provider.py` | `1079a86` |
| 散落的 `os.getenv("PROVIDER_API_KEY")` 调用 | `settings.provider_api_key` + `LiteLLMProvider(api_key=...)` | `5c5d083` |
| AgentStation 缺 `slug` / `is_builtin` / `handoff_policy` | 已加 + 迁移 0010 | `59bf1b9` |
| `services/state_machine_service`（老位置） | 仍生效，`src.runtime` 加 alias 指向 | `1079a86` |

---

## 5. 已重建（条目从这里移到"已重建"）

_（空——目前没有条目）_

---

> 最后更新：2026-09-06（V1.0-3 整理时建立）
