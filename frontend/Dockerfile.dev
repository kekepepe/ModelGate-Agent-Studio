FROM node:20-alpine

WORKDIR /app

# Install dependencies first (layer caching)
COPY package.json package-lock.json* ./
RUN npm install

# Source code is mounted at runtime for HMR

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
