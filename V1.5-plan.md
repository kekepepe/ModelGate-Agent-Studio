# V1.5 执行计划 — Evolution Quality Loop（知识主线重心）

> 立项于 2026-09-16。前置：V1.3（真实 Provider 是 LLM 式提炼的前提）。
> 主题：从"能记忆"升级为"**记忆真的改善下一次任务**"——把 V1.2 遗留的三个开环口子
> 全部闭上，并把知识提炼从规则式升级为 LLM 式。用户方向锚点：更智能、更方便、
> 反复学习沉淀知识。
> 约定不变：每子任务 1 commit + 1 push。

---

## 0. 现状盘点（探索证实：三个开环口子）

1. **`retrieved_context_items.used` 开环**：该列只在检索时被写为"是否塞进 token 预算"的
   标志，之后**无人回写**——没有 accepted/ignored、没有影响度量、没有反哺排序。
2. **提炼是规则式**：curator 的 memory/skill/经验提炼全靠硬编码规则，质量受限。
3. **无治理**：记忆冲突无检测（error_signature merge 是唯一去重）、无时间衰减、
   失败技能无自动处置；`ContextPackageSnapshot` 写后无人读；前端无
   "本次执行用了哪条历史经验"的呈现。

唯一已闭合的环：技能 success/failure 计数（`record_skill_outcome`）+ 成功率加权。

## 1. 设计决策

- **D1 效果度量三态化**：`retrieved_context_items.used` 语义升级为
  `retrieved → used → ignored`；任务收尾时由 runtime 回写：该条上下文被 worker
  实际注入（used）或被预算挤出（ignored）。
- **D2 effectiveness 归因**：任务终态映射为该次被 used 的记忆/技能的效果投票——
  `completed_verified` → +1 success，`failed/revision_required` → +1 failure，
  其余不计。聚合到 MemoryDraft/SkillDraft 的新 `effectiveness_success/failure` 计数
  （与 skill 的 success_rate 同构，复用 V1.2 加权机制）。
- **D3 LLM 优先提炼**：curator 的 project/experience 提炼改为 LLM 优先（prompt 含
  结构化输出契约，复用 handoff LLM 摘要模式：mock 路径 → 模板兜底并标注
  `generated_by='rule'`，live 路径 → `generated_by='llm'`）；**规则实现保留为兜底，
  不删除**。
- **D4 冲突治理不自动删除**：同 error_signature 但 resolution 矛盾、或同主题高相似
  低一致的记忆 → 打 `conflict` 标记 + Evolution 页人工裁决（保留 A / 保留 B /
  合并）；**永不自动删除用户记忆**。
- **D5 时间衰减**：90/180 天 `expires_at` 已有；增加 confidence 衰减——未被
  检索命中的记忆每 30 天 confidence × 0.9（下限 0.2），命中即重置计时。由
  恢复 daemon 同周期执行（复用 V1.4 的后台线程）。
- **D6 快照可读**：`context_package_snapshots` 增加只读 API（按 worker/task 查询），
  供前端"本次执行上下文"检视。

## 2. 任务清单

### 闭环 A：hit → used → impact（核心）

- [ ] QL1 migration `0014_evolution_quality`：`memory_drafts` 加
      `effectiveness_success/effectiveness_failure`（Integer default 0）+
      `conflict_state`（String，nullable：none/conflict/resolved）；`retrieved_context_items`
      加 `outcome`（String，nullable：used/ignored——`used` 布尔列保留兼容）
- [ ] QL2 runtime 回写：任务收尾（`_execute_single_task` 尾部）把本次 context 的
      retrieved items 标 `outcome='used'`，预算外候选标 `'ignored'`；
      按任务终态给被 used 的记忆/技能记 effectiveness 投票（D2 映射表）
- [ ] QL3 排序加权：`context_service._hybrid_rank` 与 `_hybrid_rank_skills` 引入
      effectiveness 权重（同 skill success_rate 公式：0.8 + 0.4×rate，无数据=1.0）
- [ ] QL4 effectiveness API：`GET /api/v1/knowledge/memories?sort=effectiveness`（含
      分解数字）；Evolution 页排序切换 + 展示

### 闭环 B：LLM 优先提炼（智能升级）

- [ ] QL5 curator 升级：project / experience 提炼走 model_gateway（provider 抽象，
      mock 模式 → 模板兜底 `generated_by='rule'`；live → `generated_by='llm'`）；
      prompt 契约输出结构化 JSON（复用 handoff 摘要模式）
- [ ] QL6 质量对比验收：同一执行痕迹在 rule/llm 两种模式下的产出断言
      （llm 路径用 stub provider 测结构合规；live 路径在 e2e-evolution 用真模型）

### 闭环 C：治理

- [ ] QL7 冲突检测：提炼时同 error_signature 的新 resolution 与既有记忆矛盾
      （正则关键词集合：recovered vs unresolved 等）→ 旧记忆 `conflict_state='conflict'`
      + Evolution 页红色标记；裁决 API `POST /knowledge/memories/{id}/resolve`
- [ ] QL8 时间衰减 + 自动禁用：恢复 daemon 周期内执行 confidence 衰减（D5）；
      skill 连续 failure ≥3 → 自动 `status='disabled'` + 待审事件
- [ ] QL9 前端：Evolution 页 conflict 徽章 + 裁决按钮 + effectiveness 排序；
      TaskDetailPanel 渲染 memory citation（数据在 memory_retrieved 日志里，补渲染）

### 闭环 D：可观测

- [ ] QL10 快照读取 API：`GET /api/v1/tasks/{task_id}/context-snapshots`（redact 后
      返回 payload 摘要 + citation）；"本次执行用了哪条历史经验"一屏可见

## 3. 验收 — `scripts/e2e-evolution.sh`（"越用越好"的机器可验证版）

1. Goal 1 注入一个会失败的 stub 场景（如 sandbox 拒绝某命令）→ 产出
   experience_memory（resolution=Unresolved 或 Recovered）→ 审批
2. Goal 2 复现同类错误 → 断言：build_context_package 检索命中该经验
   （`/context/retrieve` source_types=[memory]）+ effectiveness 投票发生
3. 断言 Goal 2 的任务在记忆辅助下完成（或 handoff 恢复），且该经验
   effectiveness_success ≥1
4. 冲突注入：Goal 3 产生相反 resolution → 断言 conflict 标记 + 裁决 API 生效
5. 全链路无 LLM 硬依赖：mock 模式全跑通（generated_by='rule'），live 模式
   （V1.3 之后）抽查 generated_by='llm'

## 4. 明确不做

- 自动删除任何用户记忆（只标记，人工裁决）
- 记忆图谱化 / 向量库外置（SQLite JSON 向量 + 暴力 cosine 继续够用）
- 跨 Goal 的全局个性化画像（user_preference 已是全局；更细粒度画像 V2.0）
- Router 学习（AgentSelectionDecision outcome 反馈）——探索确认有价值但范围膨胀，
  移至 V2.0 待定
