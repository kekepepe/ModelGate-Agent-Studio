# V1.3 执行计划 — Real Multi-Provider + MCP

> 立项于 2026-09-16。全局路线见 [ROADMAP.md](./ROADMAP.md)；前置阶段 V1.2.1 已收口。
> 用户已拍板：**Provider 策略 = OpenAI-compatible + 一家 native 协议（Anthropic）**。
> 约定不变：每个子任务 = 1 commit + 1 push；后端 pytest / ruff / 前端四连全绿才准提交。

---

## 0. 为什么先做这个（知识主线论证）

真实多 Provider 与 MCP 是"更智能、反复学习沉淀"的**原料质量**前提：V1.2 的知识沉淀学的是
mock 执行痕迹；只有真实 LLM（不同厂商的失败模式、成本、延迟）和真实外部工具（MCP 返回的
真实数据）才能产生值得沉淀的经验。V1.5 的 LLM 式提炼也依赖本阶段的真实 provider。

## 1. 探索证实的地基问题（P2 先修）

1. **per-model api_key 存而不用**：`Model.api_key`（Text）已有列，但
   `providers/provider_config.provider_api_key()` 只读 env（`PROVIDER_API_KEY`/`OPENAI_API_KEY`），
   DB 值被忽略 → 一套 env 凭证打天下，无法注册第二家。
2. **成本不可计算**：Model 只有 `cost_level`（1-5 序数），无 per-token 单价；
   `Goal.budget_cost_usd` 从未计算；FinalSummary 的 `cost.available` 恒为 False。
3. **litellm 运行时死重**：`main.py::verify_provider_config` 仅 import 校验；真实调用走
   `services/providers/`（mock / openai_compatible）。

## 2. 设计决策

- **D1** Provider 路由按 `Model.provider` 字段：`openai_compatible`（现役）→ 现有实现；
  `anthropic` → 新手写 `AnthropicProvider`（httpx ~300 行，复制 openai_compatible 模式）。
  **不启用 LiteLLM**——为接一家协议引入重依赖（含已知 httpx pin 冲突）不符合项目依赖纪律；
  手写 adapter 模式已被 openai_compatible 验证。
- **D2** 删除 litellm 依赖与 `src/providers/` 预留层（记入 deletions log）；`main.py` 的
  `verify_provider_config` 保留但简化为配置校验（不再 import litellm）。
- **D3** per-model 凭证：`provider_factory` 从 `Model.api_key`/`Model.api_base_url` 取凭证，
  **env 仅作兜底**（优先级：Model 行 > env）；key 永不入日志/序列化（复用 redact_data）。
- **D4** 定价：migration 0012 给 `models` 表加 `price_input` / `price_output`
  （USD per 1M tokens，Float nullable）；`record_usage` 计算每次调用成本；
  FinalSummary `cost.available=True`（配了价的模型才计入，未配价的保持诚实标注）。
- **D5** Anthropic 适配范围（V1.3）：`generate`（非流式）+ tool_use ↔ OpenAI tool_calls
  双向映射 + `health_check`；`stream` **暂不实现**（runtime 已按 `hasattr(provider,"stream")`
  自动降级到 `generate`），V1.4 按需补。
- **D6** MCP 命名与归属：外部工具注册为 `mcp__<server>__<tool>`，ToolDefinition 加
  `server_id` 外键列；`mcp_servers` 表持有 transport（stdio / streamable-http）、
  command_or_url、env_secrets（redact 存储）、status、health。
- **D7** MCP 权限：Station `allowed_tools` 机制复用（name 级 allowlist）；MCP 工具默认
  `risk_level="high"` 且 mutating 工具需人工审批后置为 enabled（复用记忆审批模式）。
- **D8** MCP SDK：官方 `mcp` python-sdk（stdio + streamable-http transport）；
  客户端会话按 server 惰性建立、进程内单例；断连自动重连一次，失败降级为
  `ToolDefinition.is_enabled=False` + health 事件。

## 3. P2 任务 — Real Multi-Provider

- [ ] T1 migration `0012_model_pricing`（`price_input`/`price_output` Float nullable）
      + Model 助手方法 `compute_cost(input_tokens, output_tokens) -> Optional[float]`
- [ ] T2 per-model 凭证落地：provider_factory / openai_compatible 改为
      `model.api_key 优先、env 兜底`；key 全链路 redact；测试覆盖优先级
- [ ] T3 `AnthropicProvider`（httpx）：`generate`（含 tools）+ `health_check`；
      tool_use ↔ tool_calls 映射（含 `stop_reason: tool_use` 判定）；错误分类
      （429/5xx/timeout → ProviderError 子类）；provider_factory 按 provider 路由 + 测试
- [ ] T4 成本入账：`quota_service.record_usage` 计算 `cost_usd` 并累计到 QuotaRecord
      （加 `total_cost_usd` 列，同迁移 0012）；FinalSummary `cost` 段输出真实数字；
      Goal 预算超限 → 记 runtime 决策（不硬拦，V1.4 再加拦截）
- [ ] T5 依赖收口：删除 `litellm==1.55.0` 与 `src/providers/`；httpx pin 恢复常规
      （`>=0.27`，解除 <0.29 上限——litellm 不在了）；deletions log 记录；全量测试
- [ ] T6 故障注入测试：stub transport 抛 429/5xx/timeout → 断言
      `_handle_task_failure` 走 backup_model_ids → handoff（真实机制，非 mock 特判）
- [ ] T7 `scripts/e2e-live-provider.sh`：双 Provider（一家 OpenAI-compatible + 一家
      Anthropic）跨模型执行 → 断言 token/latency/cost 落库 + 故障切换场景
      （**需用户提供两家 key**，key 只进 `backend/.env`，不入库）
- [ ] T8 real embedding 切换实测：`EMBEDDING_BACKEND=real` 下同步源 + 检索 +
      fingerprint 重嵌验证（配额内小样本）

## 4. P3 任务 — MCP Runtime

- [ ] T9 migration `0013_mcp_servers`：`mcp_servers` 表（name unique、transport
      `stdio|streamable_http`、command_or_url、env_secrets redact JSON、args JSON、
      status、last_health、last_health_at）+ `tool_definitions.server_id`（FK nullable）
- [ ] T10 `mcp_client.py`：基于官方 `mcp` SDK；connect（stdio spawn / http）、
      `list_tools()`、`call_tool(name, args)`；进程内 per-server 会话单例 +
      断连重连一次；超时与错误分类
- [ ] T11 发现与同步：`POST /api/v1/mcp/servers`（注册）、`/{id}/sync`——
      tools/list → upsert ToolDefinition（`mcp__<server>__<tool>`，inputSchema →
      parameters，默认 risk=high）；删除已消失的远端工具（soft-disable）
- [ ] T12 执行接线：ToolExecutor 对 `server_id` 非空的工具走 MCPClient 而非本地注册表；
      ToolCallRecord 照常落库（tool_output 为 MCP content 归一化）
- [ ] T13 权限与审批：MCP 工具默认 disabled，Evolution 页/API 审批后启用；
      Station allowlist 照常生效；`risk_level=high` 的 mutating 工具在 dry_run 拒绝（复用现有门）
- [ ] T14 health + 降级：`GET /api/v1/mcp/servers` 含 last_health；健康检查定时器
      （API 触发 + 执行前惰性检查）；断连 → 工具 soft-disable + 事件日志
- [ ] T15 `scripts/e2e-mcp.sh`：注册真实 filesystem server（npx
      `@modelcontextprotocol/server-filesystem`）→ sync 发现 → 审批启用 → runtime
      经由 MCP 读文件 → ToolCallRecord 断言（GitHub server 为第二批）

## 5. 验收标准

1. Model Manager 注册两家不同 provider 的模型（各配 key/price）→ 同一 Goal 的
   不同 Task 分别路由到两家真实模型并完成 → quota/cost 记录真实数字
2. 强制主模型 429 → runtime 自动 handoff 到 backup（跨 provider）→ 接受后完成
3. MCP filesystem server 注册 → sync 发现 ≥4 工具 → 审批启用 → Agent 经 MCP 读到
   workspace 文件 → ToolCallRecord 有记录 → server 停止后工具自动 soft-disable
4. `pytest -q`（含 anthropic/mcp 新测试，mock transport）/ ruff / 前端四连全绿
5. 零 mock 特判绕过：所有新路径在单测用 stub transport/stdio 覆盖，live 路径只在
   e2e-live-provider.sh 用真 key 跑

## 6. 明确不做

- LiteLLM 统一网关（决策 D1）；OAuth/多租户；MCP server 的远程市场/自动安装；
  streaming（D5，V1.4 按需）；Anthropic prompt caching / extended thinking
- browser MCP server（第二批）；MCP resources/prompts（只接 tools）
