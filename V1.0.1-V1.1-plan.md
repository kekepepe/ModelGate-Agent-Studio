# V1.0.1 → V1.1 执行计划（修复 + Handoff 完整业务流）

> 制定于 2026-09-15，基于 HEAD `63868dc`（V1.0 末态）之后的实测检查与全量代码探索。
> 本文是当前阶段的执行计划；架构层面的唯一权威仍是 `docs/design/2026-09-06-platform-redesign.md`。
> 进度用 checkbox 跟踪，每完成一个子任务在本文件打勾（commit 约定见 HANDOVER §9.1：每个子任务 = 1 commit + 1 push）。

---

## 0. 背景（探索结论）

后端 Handoff **不是从零开始**，runtime 已具备：

- 自动触发点 ×2：`runtime_service._handle_task_failure`（error + backup 路由存在 → `trigger_handoff(reason="error")`）、`_handle_quota_intercept`（quota LIMITED/COOLDOWN → `reason="quota_exceeded"`）
- 恢复机制：`WorkerSession.inherited_from_handoff_id` + `execute_task_step` 的 `can_resume_handoff` + `_execute_single_task` resume 分支
- 手动 accept / result API：`POST /handoffs/{id}/accept`、`PATCH /handoffs/{id}/result`（result 写回已接 completion gate）
- workspace 一致性校验：accept 时 SHA-256 校验 `changed_files`，冲突走 HandoffConflictError → 自动 replan

V1.1 真正缺的：

1. **LLM 摘要生成**——现在 `trigger_handoff` 同步走 fallback 模板，没有 LLM 路径，也没有 `generated_by/generated_at`
2. **handoff_policy 未被 runtime 尊重**——`AgentStation.get_handoff_policy()`（migration 0010 引入）在触发点完全没被查询
3. **验证失败后的 quality 触发点**——重试耗尽现在直接 blocked/auto-replan，没有 policy-aware 的 handoff 分支
4. **前端接线**——`/handoffs` 路由存在但导航无入口；TaskCard 有 handoff 插槽但 `ThreeZoneCardFlow` 传空 stub；`workspaceViewModel.ts:126` 检查的 `summary_ready/in_progress` 状态在后端枚举里不存在
5. **Quota Overview 页**——后端 API 齐（overview/status/patch/intercept），页面需对齐现接口

随发现随修的问题（Phase F）：select.tsx 未提交但被已提交代码引用、vitest 缺 `@` 别名、6 个测试文件断言过期、`.playwright-cli/` 误提交、CLAUDE.md/HANDOVER.md 文档失实。

## 1. 关键设计决策

- **D1** select.tsx：**提交**而非删除（HANDOVER 原建议删除的前提"无人引用"经核实不成立）。
- **D2** Handoff 状态机沿用代码现有枚举 `requested→generating_summary→ready→accepted→completed/failed`；修订设计文档 §3.2 的表述而非改代码（枚举已入库、有测试、有前端类型，改名是无价值迁移）。
- **D3** LLM 摘要：`trigger_handoff` 先建 `generating_summary`，组装 artifacts / tool_call_records / verification_results / checkpoint 上下文调用 model_gateway（LiteLLM 抽象；mock 模式直接模板兜底），产出 9 核心字段 + `changed_files` 等扩展字段 + 新增 `generated_by: 'llm'|'fallback'` 与 `generated_at`（存 `handoff_summary` JSON 列，**无需迁移**）；LLM 失败→模板兜底并标记。
- **D4** handoff_policy 接入 runtime：两个现有触发点改为先查 `AgentStation.get_handoff_policy()`（`can_initiate` / `on_quota_exhausted: handoff|fail|fallback_backup` / `on_provider_error: retry_once|handoff|fail`），新增验证失败重试耗尽时的 `on_quality_issue: handoff|replan`（默认 `replan` 保持现行为）；默认策略兜底为现行为，保证旧测试全绿。
- **D5** accept 保持设计文档的"用户在 Workspace 点接受"为主，`auto_continue` 参数保留为可选自动续跑。不做全自动无人值守交接。
- **D6** 前端 shadcn 化只做 V1.1 触达的组件（HandoffList / HandoffConfirmModal / HandoffDetailDrawer / HandoffPage 等）；其余 30+ 组件维持 HANDOVER §7.4 的"计划内欠账"，不在本阶段做。
- **D7** 真实 Provider smoke（V1.0.1 任务 #2）需要 OpenAI-compatible API key，本轮只就位脚本骨架与文档，实际执行等用户提供 key。

## 2. 阶段任务

### Phase F 修复 [V1.0.1]

- [x] F1 `fix`: 提交 `select.tsx` + `vitest.config.ts` 补 `@` 别名（`c4e8f91`）
- [x] F2 `test`: 6 个组件测试对齐 shadcn 重构后 UI；jsdom 补 PointerEvent / pointer capture / scrollIntoView polyfill（`baa6355`）
- [x] F3 `chore`: untrack `.playwright-cli/` + gitignore（`1f8134e`）
- [x] F4 `docs`: CLAUDE.md 文档路径指向设计文档；HANDOVER.md 修正 Router / select.tsx 失实项、落定 §10 三项决策；归档 `V1.0-frontend-plan.md` 并记入 deletions log（`25c1865`）

### Phase P 计划文档 [V1.0.1]

- [ ] P5 本文件（`V1.0.1-V1.1-plan.md`）+ docs/README.md 登记入口 + HANDOVER §8 指向

### Phase C CI [V1.0.1]

（按 `docs/_archive_2026/v1.0_deletions_log.md` §6 的恢复路径重建，规避已知失败原因：不硬编码脚本路径、不 pin npm 版本、不放需要 secrets 的 job）

- [ ] C6 `.github/workflows/backend-ci.yml`：paths `backend/**`；setup-python 按 `.python-version`；装 requirements(-dev)；`pytest -q`（ruff 仅当 pyproject 已配置才加）
- [ ] C7 `.github/workflows/frontend-ci.yml`：paths `frontend/**`；`npm ci → lint → typecheck → build → test`

### Phase B V1.1 Handoff 完整业务流 [V1.1]

目标闭环（对应设计 §4.5 + §8 V1.1 验收）：

```
自动/手动触发 → Summary 生成（LLM 优先，模板兜底）→ ready
  → Workspace 内用户点接受 → 新 Worker 继承上下文继续执行
  → 完成写回 result → Logs / Handoff 页全程可见
```

- [ ] B8 `feat`: LLM handoff summary 生成（D3；测试：mock 模板路径 / stub gateway 的 LLM 路径 / LLM 失败兜底路径）
- [ ] B9 `feat`: handoff_policy 接入 runtime 触发点（D4；含 quota→handoff、error→backup、quality→replan 分支测试）
- [ ] B10 `feat`: Workspace Task Card Handoff 状态条——`workspaceViewModel` 状态词汇修正为后端枚举（`requested/generating_summary/ready/accepted`）+ `ThreeZoneCardFlow` 传真实 handoff / onOpenHandoff / onRequestHandoff
- [ ] B11 `feat`: GlobalHeader 加 Handoff 导航入口 + HandoffPage/List/Modal/Drawer 对齐现 API 类型（D6 范围内 shadcn 对齐）
- [ ] B12 `feat`: Quota Overview 页对齐现接口（`/api/v1/quota/overview` + model status/patch；展示用量/限额/quota_status/handoff_triggered_count）
- [ ] B13 `test`: `scripts/e2e-handoff.sh` 验收脚本——mock 模式 PATCH quota 状态为 LIMITED → 跑 goal → 断言 auto-handoff 记录 + Logs 事件链 + accept → 任务继续 → result 写回（设计 §8 V1.1 验收）
- [ ] B14 `docs`: HANDOVER / CHANGELOG 状态推进收尾

## 3. 验证门禁

每个 commit 前本地跑：

- 后端 `pytest -q` 全绿（基线 411 passed, 1 skipped）
- 前端 `npm run test` 0 fail（修复后基线 175 passed）、`npm run typecheck`、`npm run build`
- Phase B 收尾跑 `scripts/e2e-demo.sh`（mock docker 栈）确认不回归

## 4. 风险与已知坑（执行时规避）

- `httpx>=0.27,<0.29` 宽 pin 不动（HANDOVER §7.2，litellm 1.55.0 约束 `httpx<0.28`）
- `GET /api/v1/tasks` 必须注册在 `GET /tasks/{task_id}` 之前（路径遮蔽），不动路由顺序
- Radix Select 在 jsdom 必须用 user-event 指针事件（polyfill 已在 `src/test/setup.ts`）
- `paused` 不是合法 TaskStatus（HANDOVER §7.5），前端筛选时继续排除
- `useRuntimeEvents` 的 unmount 路径要保留（HANDOVER §7.3 曾修过 listener 泄漏）

## 5. 明确不做（本阶段）

- 剩余 30+ 组件全面 shadcn 化（V1.0.2+ 计划内欠账）
- 自动无人值守 handoff（D5：保持用户确认）
- Postgres / Redis 切换、并行 Task、arq 启用（V1.3/V1.4）
- 需要 secrets 的 CI job（integration / nightly / live-provider）
