# V1.2.1 执行计划 — Architecture Alignment + CI + Product Acceptance

> 立项于 2026-09-15（ROADMAP 重构后首个阶段）。本阶段**只做收口与质量门禁，不做任何新功能**。
> 背景与阶段定义见 [ROADMAP.md](./ROADMAP.md)（P0 + P1 → V1.2.1）。
> 约定不变：每个子任务 = 1 commit + 1 push；测试全绿才准提交。

---

## 0. 为什么要这个阶段

V1.0–V1.2 三轮迭代让代码跑在了文档前面：设计文档 §8 的版本编号已被 ROADMAP 取代；
HANDOVER.md 被阶段性状态更新改得偏离"交接文档"本位；HANDOVER 对技术栈的描述存在
**已被证实的错误**（见 §1 审计）；README 宣称的每项能力没有自动化证据。P0 收口 SSOT，
P1 把"完成"变成可机器验证的事实，之后 V1.3+ 的每个版本才有可靠的比较基线。

## 1. P0 — Architecture Alignment（文档与栈决策收口）

### 1.1 三大栈决策（P0 落锤，写入设计文档后不再反复）

| 决策点 | 证据（已核实） | 建议 |
|---|---|---|
| React Router | 实际使用 react-router-dom v7 声明式 `<Routes>`（App.tsx），测试覆盖；HANDOVER 曾误写 TanStack Router 已修正 | **正式保留**，设计文档 §5 记为正式选型 |
| SQLAlchemy sync | 全库无 `create_async_engine` / `AsyncSession`（已 grep 证实）；runtime 为同步阻塞模型 | **正式保留 sync**。理由：单机 SQLite + 串行执行，async 重写是无用户价值的纯 churn；V1.4 引入并行时若成为瓶颈再立项评估 |
| LiteLLM 依赖 | `litellm==1.55.0` 在 requirements 中，但**运行时是死重**：唯一引用点 `main.py::_init_provider()` 只做启动期 import 校验且结果被丢弃（`# noqa: F841`）；真实模型调用全部走 `src/services/providers/`（provider_factory：mock / openai_compatible） | **保留依赖，V1.3 P2 恢复为多 Provider Adapter**。理由：P2 要求非 OpenAI-compatible 的第二家真实 Provider，LiteLLM 是最短路径，删除则届时要重装依赖并重踩 httpx pin 冲突。P0 只做澄清：文档标注 `src/providers/` 为 V1.3 预留层、当前 runtime 走 `src/services/providers/`，`_init_provider` 更名为 `verify_provider_config` 之类以名副其实 |

### 1.2 任务清单

- [ ] T1 设计文档收口：§8 按 ROADMAP 重写编号（V1.2.1 / V1.3 / V1.4 / V1.5 / V2.0）；§5/§6 补记
      React Router、sync SQLAlchemy、双 Provider 层（`services/providers` 为现役，`providers/` 为
      V1.3 预留）三项决策及理由
- [ ] T2 `main.py::_init_provider` 更名为 `verify_provider_config`（消除"初始化了 provider 但没人用"
      的误导），行为不变（启动期校验 + 日志），补注释说明 litellm 为 V1.3 预留
- [ ] T3 HANDOVER.md 重构为真正的交接文档：一页式结构——①项目一句话 ②当前状态快照表（版本 /
      测试 / 验收脚本）③代码地图（关键文件索引）④已知坑（保留 §7 全部怪癖条目）⑤路线图指针
      （→ ROADMAP.md）⑥工作约定。移除执行计划式的进度叙事（那是各计划文档的职责）
- [ ] T4 失实声明清扫：全库 grep 修正——"SQLAlchemy 2 async"（HANDOVER §4）、Python 版本表述
      （本地 venv 实际 3.9，.python-version / Docker 为 3.12.8，需写明差异与测试环境要求）、其它
      与代码不符的描述；顺带核实 `docs/e2e-docker-stack.md` 内容仍与现栈一致
- [ ] T5 docs/README.md 登记本计划；CHANGELOG 记 V1.2.1（文档收口部分）

## 2. P1 — Quality Gate（CI + 产品验收）

### 2.1 GitHub Actions（最小集，遵循 deletions log §6 的失败教训）

- [ ] T6 `.github/workflows/backend-ci.yml`：paths `backend/**`；setup-python 按 `.python-version`
      （3.12.8）；`pip install -r requirements.txt -r requirements-dev.txt`；`ruff check .`（pyproject
      已有 ruff 配置，需先本地确认 9 个存量违规修复或显式豁免）；`pytest -q`
- [ ] T7 `.github/workflows/frontend-ci.yml`：paths `frontend/**`；Node 22（`.nvmrc`）；
      `npm ci → lint → typecheck → test → build`；**不 pin npm 版本**（旧 CI 三大死因之一）
- [ ] T8 CI 收敛观察：两workflow 首跑绿；失败即修，不带病合并

### 2.2 e2e-product-acceptance.sh（产品级验收，mock 栈）

一条脚本自动证明 README「当前能力」的全部声明（复用 e2e-demo / e2e-handoff 的模式）：

- [ ] T9 场景 A（核心闭环）：≥3 Task、≥2 Station 的 Goal 跑完 → 断言 Router decision 日志存在、
      ToolCallRecord ≥1、Supervisor review 产出、Final Summary 7 字段齐全
- [ ] T10 场景 B（Handoff）：配额置 LIMITED → auto-handoff → accept → 恢复完成（复用 e2e-handoff 逻辑）
- [ ] T11 场景 C（Evolution）：沉淀产物落库（project / experience memory、approved skill 可选）、
      **下一次**新 Goal 的 context 包中能检索到上一轮记忆/偏好（build_context_package 含
      project_memories 或 user_preferences 非空）
- [ ] T12 README 能力声明 ↔ 证据映射表（写进本计划文档收尾节）：每行 = 能力 → 验证脚本/CI 步骤

### 2.3 明确不做（本阶段）

- 不接真实 Provider、不做 MCP、不引入并行/Redis/arq、不做任何 UI 新功能、不做 Pixel Office
- 不追求覆盖率数字门禁（阈值已在 vitest 配置里，不新增）

## 3. 验收标准

1. 两份 workflow 在 GitHub 上绿；本地 `pytest -q` / `ruff check .` / 前端四连全绿
2. `bash scripts/e2e-product-acceptance.sh`（mock 栈）全场景 PASS
3. 三份顶层文档（ROADMAP / README / HANDOVER）对同一事实只有一种说法；设计文档 §8 编号与
   ROADMAP 一致
4. 全程零新功能、零依赖变更（litellm/httpx pin 不动）

## 4. 风险与已知坑

- ruff 存量违规 9 个（M1 时实测，全部可 `--fix`）——P1 前清掉
- httpx 宽 pin 与 litellm 约束共存问题依旧存在（HANDOVER §7.2），CI 装 dev 依赖时留意
- CI 的 Python 版本用 3.12.8（.python-version），与本地 venv 3.9 的差异要在 T4 写明，避免
  "本地绿 CI 红"
- e2e-product-acceptance 的场景 C 依赖审批流：记忆需 approve 后才进上下文——脚本内直接调
  approve API，与 UI 行为一致

## 5. 能力 ↔ 证据映射（T12 收尾时填充）

| README 声明 | 自动证据 |
|---|---|
| 核心执行闭环 | 场景 A + backend-ci |
| Handoff 完整业务流 | 场景 B（= e2e-handoff） |
| Memory 沉淀与检索 | 场景 C |
| 混合检索 / 偏好注入 | backend pytest（test_context_hybrid_v12 等） |
| 前端可用 | frontend-ci（typecheck/test/build） |
