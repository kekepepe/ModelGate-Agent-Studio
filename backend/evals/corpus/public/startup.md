# Reproducible startup

Python is pinned to 3.12.8 and Node is pinned to 22.23.1. Run backend tests with `python -m pytest -q` from `backend`. Run browser acceptance with `npm run test:e2e` from `frontend` after starting the API and Vite services.
